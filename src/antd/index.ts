import { componentMap,buttonRenderer} from "./antd-components"
import { SchemaFormConfig } from '../config';

export const schemaFormAntdConfig: SchemaFormConfig = {
    componentMap: componentMap,
    buttonRenderer: buttonRenderer
}